import ScrollProgress from "@/components/ui/scroll-progress";

const ScrollProgressBar = () => {
  return (
    <div className="z-10">
      <ScrollProgress className="top-[73px] sm:top-[80px]" />
    </div>
  );
};

export default ScrollProgressBar;
